import 'package:flutter/material.dart';

class HomeShopView extends StatelessWidget {
  const HomeShopView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Home Shop View'),
      ),
    );
  }
}
